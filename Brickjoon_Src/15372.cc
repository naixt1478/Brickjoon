#include <bits/stdc++.h>
#define __fastios ios::sync_with_stdio(0),cin.tie(nullptr),cout.tie(nullptr)
using namespace std;
using lli = long long int;

int main()
{
    __fastios;
    int T;
    cin >> T;
    while(T--)
    {
        lli a;
        cin >> a;
        cout << a*a << '\n';
    }
}

// solve