#include "bits/stdc++.h"
using namespace std;

int main()
{
    long long int v,d,j,vl,jl,vk,jk;
    cin >> vk >> jk
        >> vl >> jl
        >> v >> d >> j;
    cout << (v*d*j)*(vk*jk + vl*jl); 
}