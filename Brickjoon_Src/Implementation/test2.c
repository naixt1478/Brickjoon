#include <stdio.H>
#include <stdlib.h>
#include <string.h>

int main(void)
{
    int T;
    long long int N,K;
    scanf("%d", &T);
    while(T--)
    {
        scanf("%lld %lld",&N, &K);
        
    }
    return 0;
}