#include <bits/stdc++.h>
#define _FASTIOS ios::sync_with_stdio(0),cin.tie(nullptr),cout.tie(nullptr)
using namespace std;

int main()
{
    _FASTIOS;
    char c1;
    cin >> c1;
    if(c1 == 'N' || c1 == 'n') cout << "Naver D2";
    else cout << "Naver Whale";
}

// solve