#include "bits/stdc++.h"
#define __fastios ios::sync_with_stdio(0),cin.tie(nullptr),cout.tie(nullptr)
using namespace std;

int main()
{
    __fastios;
    int n, a, sum1{}, max1{};
    cin >> n;
    stack<int> si1;
    while (n--)
    {
        cin >> a;
        if(si1.empty())
        {
            si1.push(a);
        }
        else
        {
	        
        }
    }
    cout << sum1;
}