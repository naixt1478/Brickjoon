#include "bits/stdc++.h"
using namespace std;

int main()
{
    int n,a,b;
    cin >> n;
    while(n--)
    {
        cin >> a >> b;
        if(a >= b) cout << "MMM BRAINS\n";
        else cout << "NO BRAINS\n";
    }
}

// solve